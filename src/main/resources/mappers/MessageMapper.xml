<?xml version="1.0" encoding="UTF-8"?>
<!-- MyBatis Mapper 파일임을 선언 -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.groupware.message.MessageMapper">

	<!-- 보낸 쪽지 목록 (페이징 + 첨부파일 수) -->
	<select id="selectMessageSenderList" resultType="messageVO">
		SELECT m.msg_id,
			   m.sender_id,
			   m.receiver_id,
			   u.user_name as receiver_name,
			   m.title,
			   m.content,
			   m.is_read,
			   m.sent_at,
			   <!-- 첨부파일 수 (0보다 크면 목록에서 📎 표시) -->
			   (SELECT COUNT(*) FROM ATTACH_FILE a
			     WHERE a.ref_type = 'MESSAGE' AND a.ref_id = m.msg_id AND a.deleted_at IS NULL) AS attach_cnt
	    FROM MESSAGE m
	    JOIN USERS u
	    ON m.receiver_id = u.user_id
	    WHERE m.sender_id = #{userId}
	    AND m.deleted_at IS NULL
	    ORDER BY m.sent_at DESC, m.msg_id DESC
	    OFFSET #{offset} ROWS FETCH NEXT #{size} ROWS ONLY
	</select>

	<!-- 받은 쪽지 목록 (페이징 + 첨부파일 수) -->
	<select id="selectMessageReceiverList" resultType="messageVO">
		SELECT m.msg_id,
			   m.sender_id,
			   m.receiver_id,
			   u.user_name as sender_name,
			   m.title,
			   m.content,
			   m.is_read,
			   m.sent_at,
			   (SELECT COUNT(*) FROM ATTACH_FILE a
			     WHERE a.ref_type = 'MESSAGE' AND a.ref_id = m.msg_id AND a.deleted_at IS NULL) AS attach_cnt
	    FROM MESSAGE m
	    JOIN USERS u
	    ON m.sender_id = u.user_id
	    WHERE m.receiver_id = #{userId}
	    AND m.deleted_at IS NULL
	    ORDER BY m.sent_at DESC, m.msg_id DESC
	    OFFSET #{offset} ROWS FETCH NEXT #{size} ROWS ONLY
	</select>

	<!-- 보낸 쪽지 총 개수 (페이지 수 계산용) -->
	<select id="countMessageSenderList" parameterType="String" resultType="int">
		SELECT COUNT(*) FROM MESSAGE
		WHERE sender_id = #{userId} AND deleted_at IS NULL
	</select>

	<!-- 받은 쪽지 총 개수 (페이지 수 계산용) -->
	<select id="countMessageReceiverList" parameterType="String" resultType="int">
		SELECT COUNT(*) FROM MESSAGE
		WHERE receiver_id = #{userId} AND deleted_at IS NULL
	</select>
	
	<!-- 쪽지 작성 -->
	<insert id="insertMessage" parameterType="messageVO">
		<selectKey keyProperty="msgId" resultType="int" order="BEFORE">
	        SELECT seq_message.NEXTVAL FROM DUAL
        </selectKey>
	    INSERT INTO MESSAGE (msg_id, sender_id, receiver_id, title, content, is_read)
	    VALUES (#{msgId}, #{senderId}, #{receiverId}, #{title}, #{content}, 'N')
	</insert>
	
	<!-- 받은 쪽지 조회 -->
	<select id="selectMessageReceiverDetail" resultType="MessageVO">
		SELECT m.msg_id,
			   m.sender_id,
			   m.receiver_id,
			   u.user_name as sender_name,
			   u2.user_name as receiver_name,
			   m.title,
			   m.content,
			   m.is_read,
			   m.sent_at
	    FROM MESSAGE m
	    JOIN USERS u
	    ON m.sender_id = u.user_id
	    JOIN USERS u2
	    ON m.receiver_id = u2.user_id
	    WHERE m.receiver_id = #{userId}  
	    AND m.msg_id = #{msgId}
	</select>
	
	<!-- 보낸 쪽지 조회 -->
	<select id="selectMessageSenderDetail" resultType="MessageVO">
		SELECT m.msg_id,
				   m.sender_id,
				   m.receiver_id,
				   u.user_name as receiver_name,
				   u2.user_name as sender_name,
				   m.title,
				   m.content,
				   m.is_read,
				   m.sent_at
		    FROM MESSAGE m
		    JOIN USERS u
		    ON m.receiver_id = u.user_id
		    JOIN USERS u2
		    ON m.sender_id = u2.user_id
		    WHERE m.sender_id = #{userId}  
		    AND m.msg_id = #{msgId}
	</select>
	
	<!-- 읽음 표시 기능 -->
	<update id="updateMessageReadStatus" parameterType="MessageVO" >
		UPDATE MESSAGE 
		SET is_read = 'Y' 
		WHERE msg_id = #{msgId} 
		AND receiver_id = #{receiverId}
	
	</update>
	
	<!-- 쪽지 수신인 이름 검색용(자동완성) -->
	<select id="selectReceiverName" parameterType="String" resultType="com.groupware.user.UserVO">
	    SELECT user_id   as userId,
	           user_name as userName
	    FROM USERS
	    WHERE user_name LIKE '%' || #{userName} || '%'
	</select>

	<!-- 쪽지 첨부파일 등록 (공용 ATTACH_FILE, ref_type='MESSAGE') -->
	<insert id="insertAttachFile" parameterType="com.groupware.message.AttachFileVO">
	    INSERT INTO ATTACH_FILE (file_id, ref_type, ref_id, orig_name, save_path, file_size)
	    VALUES (seq_attach.NEXTVAL, #{refType}, #{refId}, #{origName}, #{savePath}, #{fileSize})
	</insert>

	<!-- 쪽지 첨부파일 목록 조회 -->
	<select id="selectAttachFileList" parameterType="int" resultType="com.groupware.message.AttachFileVO">
	    SELECT file_id,
	           ref_type,
	           ref_id,
	           orig_name,
	           save_path,
	           file_size
	    FROM ATTACH_FILE
	    WHERE ref_id = #{refId}
	    AND ref_type = 'MESSAGE'
	    AND deleted_at IS NULL
	</select>

	<!-- 쪽지 첨부파일 단건 조회 (다운로드용) -->
	<select id="selectAttachFile" parameterType="int" resultType="com.groupware.message.AttachFileVO">
	    SELECT file_id,
	           ref_type,
	           ref_id,
	           orig_name,
	           save_path,
	           file_size
	    FROM ATTACH_FILE
	    WHERE file_id = #{fileId}
	    AND deleted_at IS NULL
	</select>
	
	<!-- 회수 기능할 때 삭제 기능 구현 지금은 db 컬럼 추가 기능 구현 해야함으로 기능 제외함 -->
	<!--  보낸 쪽지 삭제 
	 <update id="deleteMessageBySender" parameterType="messageVO">
	  	UPDATE MESSAGE
	  	SET deleted_at = SYSDATE
	  	WHERE msg_id = #{msgId}
	  	AND sender_id = #{senderId}
	 </update>-->
	 
	<!-- 받은 쪽지 삭제  
	 <update id="deleteMessageByReceiver" parameterType="messageVO">
	 	UPDATE MESSAGE
	  	SET deleted_at = SYSDATE
	  	WHERE msg_id = #{msgId}
	  	AND receiver_id = #{receiverId}
	 </update>--> 
		
</mapper>